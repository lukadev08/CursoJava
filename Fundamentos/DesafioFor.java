public class DesafioFor {
    public static void main(String[] args) {
        // fazendo contagem em loop sem usar numeros:
    for (String v = "#"; !v.equals("######"); v += "#"){
                    // delimita valor diferente na quantidade // += incrementa com simbolo ate limite
        System.out.println(v);
    }
            }
}
